# Frontend implementation progress

> [!CAUTION]
> This is a historical implementation ledger, not a current readiness statement or live runbook. Its earlier “built,” “verified,” “safe interruption,” migration, and live-backfill conclusions were written before the 2026-07-22 full-system review found release-critical cross-entry-point defects. Do not resume the launcher or use the recorded live commands. Current status is [SAFETY_HOLD.md](SAFETY_HOLD.md); the replacement evidence requirements are [RELEASE_GATES.md](RELEASE_GATES.md), and the complete findings/actions are [FINDINGS_REGISTER.md](FINDINGS_REGISTER.md) and [ACTION_PRIORITY_MATRIX.md](ACTION_PRIORITY_MATRIX.md).

The factual implementation/test chronology below is retained for provenance. Where it conflicts with the later review, the later safety documents control.

Historical plan status: Stage 11 was marked complete under the stage-local criteria used at the time. Current independent status: **safety hold; no live backfill/launcher/maintenance is authorized**.

This file is the durable handoff ledger for `docs/FRONTEND_PLAN.md`. Update it at the end of every implementation stage before stopping. Do not mark a stage complete unless its acceptance criteria and verification gates have actually passed.

## Status legend

- `Not started`: no product implementation work has begun.
- `In progress`: work is confined to this stage; later stages must not be started.
- `Blocked`: progress cannot continue safely; record the exact blocker and evidence.
- `Complete`: the historical stage ledger recorded its then-defined criteria as passed; it does not mean later cross-entry-point safety review or current release gates passed.

## Planning baseline

- Approved specification: `docs/FRONTEND_SPEC.md`
- Approved implementation plan: `docs/FRONTEND_PLAN.md`
- Permanent repository rules: `AGENTS.md`
- Planning-time SQLite schema: version 2
- Current implemented and live-vault schema after Stage 11: version 12; supported version-2 through version-11 manifests remain readable until explicitly migrated
- Existing frontend: separate read-only FastAPI dashboard with vanilla HTML/CSS/JavaScript
- Existing backend guarantees to preserve: immutable source, content-addressed canonical objects, triple hashing, byte comparison, collision separation, atomic no-overwrite publication, append-only source history, recovery sidecars/exports, and single-writer safety
- Live-vault planning observation: approximately 3.52 GB manifest; import `run_20260720T174333Z_2d2c654345b4` was active with a 1,374,328-file traversal and 146,034 candidate assets. Treat this as historical planning evidence; re-check writer state before any implementation-time maintenance.
- Baseline verification observed during planning: `6 passed`, Ruff clean, with one Starlette TestClient deprecation warning.
- No screenshots were taken or inspected during repository inspection/planning.

## Stage tracker

| # | Stage | Depends on | Effort | Status |
|---:|---|---|---|---|
| 1 | Governance, documentation, migrations, and immutable boundaries | None | Extra High | Complete |
| 2 | Import batch discovery and review manifest | 1 | Extra High | Complete |
| 3 | Reviewed copying, durable telemetry, and import history | 2 | Extra High | Complete |
| 4 | Preprocessing, derivatives, metadata, and feature pipeline | 3; review previews also use 2 | Extra High | Complete |
| 5 | Separate SvelteKit application and API foundation | 1 and 4 | Extra High | Complete |
| 6 | Import progress, review, history, and manifest interface | 2, 3, 4, 5 | Extra High | Complete |
| 7 | Virtualized library, contact sheet, actions, filtering, and inspector | 3, 4, 5 | Extra High | Complete |
| 8 | Calendar, folder, equipment, and private map views | 4 and 7 | Extra High | Complete |
| 9 | Similarity Stacks and best-frame ranking | 4, 5, 7 | Extra High | Complete |
| 10 | Explainable junk filtering, feedback, and bulk reject | 4, 5, 7, 9 | Extra High | Complete |
| 11 | Backfill, compatibility, performance, accessibility, and release verification | 1–10 | Extra High | Complete |

## Historical end-of-plan conclusion — superseded

The original ledger concluded that Stage 11 and its Stage 1–11 acceptance/verification gates had passed and that the approved plan contained no later implementation stage. The 2026-07-22 full-system review found release-critical behaviours those stage-local gates did not cover.

Current controlling conclusion: the application build and historical test results are preserved, but the live-vault backup/recovery and paused/resumable claims are not sufficient. Resuming the persisted backfill or launcher is **not authorized maintenance**. Follow [SAFETY_HOLD.md](SAFETY_HOLD.md) and [RELEASE_GATES.md](RELEASE_GATES.md).

## Permanent implementation gates

- Do not modify, move, rename, overwrite, or delete source files or canonical vault objects.
- Keep reject/favourite/rating/exclusion/grouping/feedback actions metadata-only.
- Do not calculate media data in frontend code or HTTP request handlers.
- Serve only ready persisted derivatives; enqueue missing work outside the request.
- Do not take, generate, save, compare, or inspect screenshots.
- Configure browser tests with screenshots, video, and traces disabled.
- Preserve the existing dashboard, CLI commands, tests, recovery behavior, and safety guarantees.
- Do not migrate the live vault while a writer is active.
- Test migrations and backfills against a copied database before the live vault.
- Stop at the current stage's explicit stopping point.

## Required stage completion record

At the end of each stage, append a dated record containing:

1. Stage number and final status.
2. Objective achieved.
3. Files/components changed.
4. Schema/API/CLI/interface contracts added or changed.
5. Acceptance criteria with pass/fail evidence.
6. Exact test, lint, type-check, build, query-plan, and audit commands run.
7. Source and canonical-object immutability evidence.
8. Screenshot prohibition verification, including confirmation that no screenshot/video/trace artifacts were created.
9. Known limitations, deviations, or follow-up decisions.
10. Explicit confirmation that work stopped before the next stage.

Use this template:

```markdown
### YYYY-MM-DD — Stage N: title

- Status: Complete | In progress | Blocked
- Objective result:
- Changed:
- Contracts:
- Acceptance evidence:
- Verification commands/results:
- Immutability audit:
- No-screenshot audit:
- Decisions/limitations:
- Stopping point respected: Yes/No — explanation
- Next authorized stage:
```

## Approved decision log

### D-001 — Top-level inbox batches

- Decision: default to configurable `G:\MediaVaultImports`; each immediate child folder is one recursive batch.
- Reason: creates stable review/history boundaries without turning a rolling folder into an ambiguous endless import.

### D-002 — Separate SvelteKit application

- Decision: Svelte 5 + SvelteKit + TypeScript, statically built and served by a new FastAPI app on port 8766.
- Reason: concise compiled components, maintainable routing/state, static FastAPI hosting, and supported virtualization for the required interaction density.
- Compatibility: preserve current `ui` dashboard on port 8765.

### D-003 — Offline/private map

- Decision: bundled offline world basemap plus persisted clusters; no external tiles.
- Reason: avoids location leakage, network dependencies, and CSP exceptions.

### D-004 — Background materialization

- Decision: arbitrary compound sorts, seeded random views, Stack profiles, and junk profiles are materialized by durable jobs.
- Reason: prevents expensive request-time/frontend calculations while retaining configurable behavior.

### D-005 — Honest junk semantics

- Decision: use `possible obstruction or accidental frame` and `focus deficit` until narrower claims have validated evidence.
- Reason: avoids misleading confidence while preserving the review goal.

### D-006 — Transparent Stack evidence

- Decision: use perceptual/time/equipment evidence and indexed candidate search; defer opaque heavyweight embeddings.
- Reason: improves explainability, scale, maintenance, and offline operation.

### D-007 — Sidecars and random files

- Decision: recognized associated sidecars are verified opaque assets when their media is included; unrelated non-media remains manifested but excluded by default.
- Reason: preserves meaningful edit/provenance files without importing arbitrary junk.

### D-008 — Video scope

- Decision: show video posters and metadata; defer original-video streaming.
- Reason: the approved goal is photo review, and streaming broadens media-serving/performance risk without being necessary.

## Validation commands

Run the commands applicable to the current stage. Do not use screenshot flags or visual snapshot tooling.

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m ruff check .
```

```powershell
Set-Location .\review_ui
npm ci
npm run check
npm test
npm run build
npm run test:e2e
npm run test:a11y
npm audit --omit=dev --audit-level=low
```

## Documentation-only initialization record

### 2026-07-20 — Approved plan persistence

- Status: Complete (planning documents only; no product stage completed)
- Objective result: persisted the approved specification, dependency-ordered plan, progress ledger, and permanent repository rules.
- Changed: `AGENTS.md`, `docs/FRONTEND_SPEC.md`, `docs/FRONTEND_PLAN.md`, `docs/FRONTEND_PROGRESS.md`.
- Contracts: documentation only; no runtime API, schema, CLI, backend, or frontend changes.
- Acceptance evidence: documentation audit confirmed all 11 numbered stages, dependencies, per-stage objective/backend/frontend/acceptance/tests/stopping sections, assumptions, deviations, effort recommendations, and permanent no-screenshot/immutability/preprocessing rules.
- Verification commands/results: PowerShell text audit passed with `CROSS_CHECK_OK stages=11 per_stage_sections=complete`; all four target files exist, are non-empty UTF-8 text, and no runtime file was changed by this documentation-only operation.
- Immutability audit: no source or vault media operations authorized or performed.
- No-screenshot audit: no screenshots taken, generated, saved, compared, or inspected.
- Decisions/limitations: product implementation intentionally not started.
- Stopping point respected: Yes — stop after planning document validation.
- Next authorized stage: Stage 1 only.

### 2026-07-21 — Stage 1: Governance, documentation, migrations, and immutable boundaries

- Status: Complete
- Objective result: Replaced implicit schema rewriting with an ordered, audited, explicit migration framework; added a safe `media-vault migrate --vault ...` command; defined typed review configuration and immutable/regenerable storage boundaries; and proved the framework only against isolated synthetic schema-v2 copies.
- Changed: `media_vault/db.py`, `media_vault/migrations.py`, `media_vault/config.py`, `media_vault/core.py`, `media_vault/cli.py`, `media_vault/vault_ops.py`, `tests/test_migrations.py`, `tests/test_ui.py`, `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 3 adds only `schema_migrations(version, name, applied_at, tool_version)`; supported v2 manifests remain readable by legacy commands and are never migrated on open; new feature callers can require v3 and receive an explicit migration-required error; migration holds `active-writer.lock`, validates the input, checks free space, creates and validates a unique SQLite backup in `state/backups`, applies ordered migrations transactionally, validates version/foreign keys/integrity, restores the verified backup after a post-commit validation failure, and is a validated no-op when repeated; JSONL exports now record the open database's actual schema version. `ReviewConfig`, `WorkerLimits`, `RequestBudgets`, and `AnalyzerVersions` define the vault, inbox (`G:\MediaVaultImports`), derivative root, ports 8765/8766, localhost binding, worker limits, request budgets, analyzer versions, and source/vault/inbox/derivative/canonical separation rules without adding runtime review features.
- Acceptance evidence: `test_v2_opens_without_implicit_migration_and_review_features_refuse` and `test_representative_legacy_commands_keep_v2_schema` prove v2 compatibility and explicit feature gating; fresh/repeated and v2-copy migration tests prove deterministic v3 initialization, a verified v2 backup, the audit row, and idempotence; live-writer, insufficient-space, backup-failure, transactional failure, `KeyboardInterrupt`, foreign-key failure, post-migration validation/restore, and initializer-rewrite tests pass. The existing dashboard and legacy end-to-end tests remain green. The governance cross-check reports `FRONTEND_DOC_AUDIT_OK stages=11 dependency=none safety=aligned decisions=aligned`.
- Verification commands/results: pre-edit `.\.venv\Scripts\python.exe -m pytest -q` -> `6 passed, 1 warning in 50.32s`; pre-edit `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`; final `.\.venv\Scripts\python.exe -m pytest -q tests/test_migrations.py` -> `15 passed in 14.81s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `21 passed, 1 warning in 31.71s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`; `.\.venv\Scripts\python.exe -m media_vault migrate --help` -> exit 0 with the required `--vault` option. The documentation/capture/artifact audit command reported `FRONTEND_DOC_AUDIT_OK`, `TEST_CAPTURE_CONFIG_AUDIT_OK`, and `BROWSER_ARTIFACT_AUDIT_OK`. TypeScript, Svelte, Vitest, production-build, Playwright, accessibility, and query-plan checks are not applicable because Stage 1 deliberately adds no frontend project, HTTP API, or new query surface. The one full-suite warning is the approved pre-existing Starlette TestClient deprecation; no dependency churn was introduced. An intermediate focused run exposed Windows' requirement for a writable handle when calling `fsync` and a test-fixture column-count error; both were corrected before the final passing runs.
- Immutability audit: every migration/control test uses a temporary synthetic source and a pre-existing synthetic canonical object, snapshotting relative paths, size, mtime nanoseconds, ctime nanoseconds, mode, and SHA-256 before and after; all snapshots matched. Migration code reads and writes only SQLite state/backup files and the writer lock. No real source, live manifest, or canonical vault object was read, hashed, copied, modified, moved, renamed, overwritten, or deleted, and no live-vault migration was attempted.
- No-screenshot audit: no screenshot, video, browser trace, browser automation, or visual snapshot operation was used. Configuration search found no capture settings, and the repository artifact-name audit found no screenshot/video/trace output (`TEST_CAPTURE_CONFIG_AUDIT_OK`; `BROWSER_ARTIFACT_AUDIT_OK`).
- Decisions/limitations: v3 is intentionally governance-only and contains no import, job, derivative, API, or UI tables. Fresh manifests are created at v3; existing v2 manifests stay v2 until the explicit command succeeds. Backups remain as durable metadata evidence and are not removed automatically. Live-vault rollout remains deferred to Stage 11, so Stage 1 proves migration only on isolated synthetic copies. The Starlette warning remains documented and unchanged. The initial worktree had only `README.md` tracked and the remaining repository files untracked, so Git could not provide a conventional complete diff; final review used the known pre-edit inspection, edited-file review, `git diff --check` for the tracked change, Ruff/tests, and an explicit Stage 2+ implementation search.
- Stopping point respected: Yes — stopped after a schema-v2 copy migrated safely and all governance gates passed; no import tables, worker behavior, API routes, or UI components were added.
- Next authorized stage: Stage 2 only — Import batch discovery and review manifest.

### 2026-07-21 — Stage 2: Import batch discovery and review manifest

- Status: Complete
- Objective result: Added a complete headless reviewed-inbox discovery workflow that models each immediate child of the configured inbox as a stable batch, persists every recursively observed file/folder/reparse/other entry before any copy decision, records append-only observation and decision history, materializes exact folder/batch rollups, and reaches a durable `awaiting_review` manifest without creating or publishing canonical media.
- Changed: `media_vault/db.py`, `media_vault/review_imports.py`, `media_vault/core.py`, `media_vault/cli.py`, `tests/test_migrations.py`, `tests/test_review_imports.py`, `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 4 adds `import_batches`, `import_items`, `import_item_observations`, `import_item_decisions`, `import_folder_progress`, and the minimal discovery subset of `background_jobs`, with manifest/classification/decision/hash indexes and persisted classification/match rollups. Supported schema-v2 and schema-v3 manifests remain readable by legacy commands and are never migrated on open; Stage 2 callers require v4. `ib1`, `ii1`, `iob1`, and `job1` identifiers are deterministic. Persisted phases are truthfully separated as `discovering`, `hashing`, and `matching`, followed by `awaiting_review` or durable `interrupted`/`failed`. `ImportDiscoveryService` and the single-writer-guarded `discover_review_manifests` orchestration validate storage boundaries, enforce the shared atime guard, avoid reparse/symlink traversal, classify and triple-hash in background execution, match existing or in-batch exact content with byte comparison when a representative is available, associate recognized sidecars, and record RAW/JPEG candidate evidence. `ImportManifestService` provides typed batch summary, folder rollup, bounded indexed keyset page/filter, query-plan, optimistic decision, and append-only decision-history contracts. Include/exclude changes update SQLite metadata only.
- Acceptance evidence: `test_complete_review_manifest_decisions_rescan_and_immutability` covers top-level batch discovery, recursive files/folders, photo/video/RAW, paired and unpaired sidecars, random non-media, Unicode, multiple/unusual extensions, corrupt candidates, reparse skipping, existing-vault and in-batch exact duplicates, signature/classification/stat evidence, RAW/JPEG candidates, conservative proposals, bounded keyset/filter pages, exact root/batch rollup reconciliation, optimistic decision conflicts, restart/rescan persistence, unchanged-observation deduplication, changed-file observation history, and zero new canonical objects. `test_interruption_permission_failure_and_resume_without_duplicates` proves durable interruption, recursive permission-failure evidence, conservative incomplete-traversal handling, successful resume, stable item identities, cleared recovered errors, and no duplicated items. `test_million_row_manifest_uses_indexed_keyset_query` creates exactly 1,000,000 manifest rows and proves an indexed classification/keyset seek with no manifest scan or temporary ordering B-tree. `test_v3_copy_migrates_to_stage_2_schema` proves the additive v3-to-v4 migration on an isolated copied database with a verified v3 backup; the full migration fault/safety suite remains green. The root folder's item/file/folder/other, byte, decision, classification, match, and error rollups reconcile exactly with the batch.
- Verification commands/results: pre-edit `.\.venv\Scripts\python.exe -m pytest -q` -> `21 passed, 1 warning in 45.28s`; pre-edit `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`; final `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py` -> `16 passed in 11.15s`; final `.\.venv\Scripts\python.exe -m pytest -q tests\test_review_imports.py` -> `3 passed in 12.35s`; isolated million-row query-plan test -> `1 passed in 12.48s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `25 passed, 1 warning in 38.82s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. The schema/stage-boundary/capture/artifact audit command reported `STAGE2_SCHEMA_AUDIT_OK version=4 required_tables=6 later_stage_tables=0 integrity=ok foreign_keys=0`, `STAGE_BOUNDARY_AUDIT_OK asset_writes=0 destination_writes=0 publication=0 deletion=0 http_routes=0`, `TEST_CAPTURE_CONFIG_AUDIT_OK enabled_screenshots=0 enabled_videos=0 enabled_traces=0`, and `BROWSER_ARTIFACT_AUDIT_OK screenshots=0 videos=0 traces=0`; the final ledger cross-check reported `FRONTEND_PROGRESS_AUDIT_OK completed=2 next=3 record_fields=11 dependencies=stage1 safety=aligned`. `git diff --check` exited 0; Git emitted only advisory LF-to-CRLF working-copy notices. TypeScript/Svelte checking, Vitest, production build, Playwright, and accessibility validation are not applicable because Stage 2 adds no frontend project, browser automation, HTTP API, or UI. The one full-suite warning is the approved pre-existing Starlette TestClient deprecation and is unchanged.
- Immutability audit: all discovery/control tests use isolated temporary synthetic inbox, legacy-source, vault, and pre-existing canonical-object fixtures. Snapshots cover every relative path/directory entry, entry kind, size, mtime nanoseconds, ctime nanoseconds, file attributes, and SHA-256 file content before and after batch discovery, classification/hashing/matching, decisions, interruption, permission failure, restart, and rescan; all operation-bounded snapshots matched. The changed-file test intentionally changes its synthetic input before the rescan, then proves the rescan makes no further filesystem change. Pre-existing canonical-object snapshots remain identical and object counts do not increase. The service audit found no asset/destination insertion, filesystem copy/publication/deletion, or HTTP route. No real inbox, immutable source, live manifest, or canonical vault object was enumerated, read, hashed, copied, modified, moved, renamed, overwritten, or deleted, and no live-vault migration was attempted.
- No-screenshot audit: no screenshot, video, browser trace, browser automation, visual snapshot, or capture-producing command was used. Configuration and artifact-name audits found no enabled capture settings and no screenshot/video/trace artifacts (`TEST_CAPTURE_CONFIG_AUDIT_OK`; `BROWSER_ARTIFACT_AUDIT_OK`).
- Decisions/limitations: Stage 2 is intentionally headless. The public `inbox-scan`/worker/review API commands remain deferred to Stage 5, and no UI exists. `background_jobs` contains only discovery phase/status/evidence; claiming, leases, retries, pause/resume/cancel, copy telemetry, and import history are Stage 3. No approved copying, rich telemetry, preview/derivative generation, or visual-similarity analysis is present. Missing non-exact visual evidence is not fabricated; RAW/JPEG filename evidence and exact full-content outcomes are persisted now, while preprocessing/similarity materialization remains assigned to later approved stages. Associated sidecars are recognized through the documented local extension/name rules and unrelated sidecar-like/random files remain excluded. Existing v2/v3 databases require the explicit guarded migration before Stage 2 features can operate. No live-vault rollout or backfill was performed.
- Stopping point respected: Yes — stopped with a complete headless review manifest and metadata-only decision service; no approved copying, Stage 3 telemetry/control behavior, preview generation, import UI, API route, or later-stage feature was added.
- Next authorized stage: Stage 3 only — Reviewed copying, durable telemetry, and import history.

### 2026-07-21 — Stage 3: Reviewed copying, durable telemetry, and import history

- Status: Complete
- Objective result: Added a complete headless reviewed-import execution workflow that snapshots an exact manifest approval, requires a separate explicit execute authorization, copies only eligible approved media and associated sidecars through the existing immutable verified-publication contract, and retains reconciled job/control/telemetry/error/history evidence across pause, cancellation, failure, lease loss, and restart.
- Changed: `media_vault/db.py`, new `media_vault/review_copy.py`, `tests/test_migrations.py`, new `tests/test_review_copy.py`, `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 5 additively completes `background_jobs`, extends `import_batches`, `import_items`, and `import_folder_progress`, and adds `import_batch_approvals`, `import_approval_items`, `background_job_attempts`, `import_item_copy_attempts`, `import_progress_samples`, `import_events`, `import_errors`, and `legacy_import_history` plus bounded claim/copy indexes. Versions 2–4 remain readable by legacy commands without implicit migration; Stage 3 callers require v5. `ReviewedImportService` provides revision/observation-bound approval, a second execute gate, capacity recheck, durable claim/lease/heartbeat/retry/pause/resume/cooperative-cancel controls, safe copy execution, exact outcome reconciliation, bounded sample/event/error reads, and persisted ETA confidence. `run_reviewed_copy_job` owns the existing single-writer lock. New assets/source versions/destinations use the existing triple-hash identity, source-change check, reopened-temp hash, byte comparison, fsync, atomic no-overwrite hard link, collision discriminator, destination-conflict, and recovery-sidecar contracts. Legacy import metrics are backfilled only from retained SQLite run summaries, reports, progress JSON, or JSONL and missing fields are stored with `not recorded by this version`.
- Acceptance evidence: `test_reviewed_copy_requires_two_gates_and_reconciles_history_and_immutability` proves approval alone and stale free space cannot copy, only the separately authorized eligible set runs, associated sidecars copy, excluded/unrelated/structural outcomes create no asset, exact duplicates reuse canonical content, batch/root counters and byte totals reconcile, progress/events persist, and every ready object matches all three expected hashes. `test_destination_conflict_is_recorded_without_overwrite` proves an unexpected canonical path is never overwritten and produces structured non-retryable conflict evidence. `test_lock_owning_copy_entry_point_refuses_a_live_writer` proves the public execution service cannot bypass the existing active-writer guard, then completes under the same entry point after the synthetic lock is released. Seven parameterized restart cases cover pre-copy, temp creation, post-fsync, reopened-temp hash, byte comparison, post-publication/pre-commit, and post-telemetry boundaries; every retry produces one canonical object per distinct identity with prior job attempts retained and no partial file left. Pause/resume stops at a safe unpublished boundary; cancellation retains a verified object, stops future items, and a new explicit approval completes without changing it. Lease expiry re-queues bounded work and preserves the interrupted attempt; the claim query uses `idx_background_jobs_claim`. Pure ETA fixtures prove all four approved confidence labels. Approval-revision conflict and evidence-only legacy-null behavior pass. The v4-copy migration test proves all eight required Stage 3 tables, no Stage 4+ tables, and clean integrity/foreign keys; the v2/v3/failure/restore migration suite and all legacy import, rerun, sidecar, export, validation, dashboard, and recovery tests remain green.
- Verification commands/results: the pre-edit `.\.venv\Scripts\python.exe -m pytest -q` initially required the managed sandbox approval and then reported `25 passed, 1 warning in 38.04s`; pre-edit `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. Final `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py` -> `17 passed in 9.35s`; final `.\.venv\Scripts\python.exe -m pytest -q tests\test_review_copy.py` -> `13 passed in 9.71s`; focused Stage 1/2 compatibility `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py tests\test_review_imports.py` -> `20 passed in 24.35s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `39 passed, 1 warning in 48.63s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. The Stage 3 schema/scope and filesystem-mutation audit reported `STAGE3_SCHEMA_SCOPE_AUDIT_OK version=5 required_tables=8 later_stage_tables=0 http_routes=0` and `STAGE3_FS_MUTATION_AUDIT_OK canonical_publish=atomic_no_overwrite temp_cleanup_only=true source_delete=0 canonical_delete=0`. The clean capture/artifact audit reported `TEST_CAPTURE_CONFIG_AUDIT_OK enabled_screenshots=0 enabled_videos=0 enabled_traces=0 files_checked=22` and `BROWSER_ARTIFACT_AUDIT_OK screenshots=0 videos=0 traces=0 files_checked=47`; the ledger cross-check reported `FRONTEND_PROGRESS_AUDIT_OK completed=3 next=4 record_fields=11 dependencies=stage1+stage2 safety=aligned`. `git diff --check` exited 0 with only advisory LF-to-CRLF working-copy notices. TypeScript/Svelte, Vitest, production-build, Playwright, accessibility, and HTTP request-budget checks are not applicable because Stage 3 deliberately adds no frontend project, browser automation, HTTP route, or UI. The unchanged warning is the approved pre-existing Starlette TestClient deprecation.
- Immutability audit: all Stage 3 operations use isolated temporary synthetic inbox, vault, and pre-existing canonical fixtures. Approval, free-space refusal, copy, duplicate reuse, destination conflict, all seven crash boundaries, restart, retry, pause/resume, cancellation/reapproval, lease recovery, and legacy-history tests snapshot every synthetic inbox path/directory entry, entry kind, size, mtime/ctime nanoseconds, file attributes, and SHA-256 content around the operation; all snapshots match. Pre-existing canonical objects additionally retain exact size, mtime/ctime, attributes, and SHA-256. Authorized tests create only the expected content-addressed canonical objects; reruns/adoption never add duplicates. Conflict bytes remain unchanged. Cancellation/interruption removes only unpublished Stage 3 `.partial` files; no source or canonical object is modified, moved, renamed, overwritten, or deleted. Recovery sidecars are separate metadata records. No real inbox, immutable source, live manifest, or canonical vault object was enumerated, read, hashed, copied, modified, moved, renamed, overwritten, or deleted, and no live-vault migration/backfill was attempted.
- No-screenshot audit: no screenshot, video, browser trace, browser automation, visual snapshot, or capture-producing command was used. The clean source-configuration and artifact-name audits found no enabled capture setting or screenshot/video/trace artifact. Synthetic PNGs are numerical media corpus fixtures for immutable-copy tests, not screenshots, and no image was visually inspected or compared.
- Decisions/limitations: Stage 3 remains headless. Public `worker`, `inbox-scan`, review HTTP APIs, and UI commands remain assigned to Stage 5; the lock-owning Python service is the current execution API. No preview or derivative is generated. Metadata and thumbnail progress plus non-exact visual-duplicate evidence are explicitly null with a Stage 4/not-recorded reason; the worker does not fabricate a `thumbnailing` phase, and `indexing` refers only to the durable core asset/source/destination linkage completed here. The existing conservative inclusion policy still permits explicitly included unsupported/corrupt media candidates; excluded candidates never copy. The 10 GiB-or-5% approval margin is rechecked immediately before enqueue and each item also has a publication-boundary free-space guard. Reapproval creates new durable approval/job evidence and retains prior decisions, attempts, events, errors, and verified objects. Existing v2/v3/v4 manifests require the explicit guarded migration before Stage 3 use. No live rollout or legacy-vault backfill was executed.
- Stopping point respected: Yes — stopped with a fully auditable headless reviewed batch and safe service entry point; no Stage 4 media preprocessing/derivative table or generator, request-time media work, HTTP route, public reviewed-import CLI command, Svelte project, or UI component was added.
- Next authorized stage: Stage 4 only — Preprocessing, derivatives, metadata, and feature pipeline.

### 2026-07-21 — Stage 4: Preprocessing, derivatives, metadata, and feature pipeline

- Status: Complete
- Objective result: Added a complete headless, durable preprocessing pipeline that creates every currently approved browser-visible media representation and base metadata/quality input outside HTTP/frontend execution, persists ready/preparing/error/stale state before display, and converts the legacy preview endpoint to bounded read-only lookup plus existing-file serving.
- Changed: `media_vault/db.py`, new `media_vault/preprocess.py`, new isolated decoder helper `media_vault/_decode_worker.py`, `media_vault/metadata.py`, `media_vault/review_copy.py`, `media_vault/ui_server.py`, `media_vault/ui/app.js`, `media_vault/cli.py`, `pyproject.toml`, `tests/test_migrations.py`, new `tests/test_preprocess.py`, `tests/test_review_copy.py`, `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 6 additively adds only `derivatives`, `asset_extended_metadata`, and `asset_features`, plus indexed preprocessing job claims; versions 2–5 remain readable without implicit migration and Stage 4 callers require v6. `d1`, `aem1`, and `af1` identities bind outputs to the exact subject, analyzer, selected representation, and immutable input identity. `PreprocessingService` queues/claims/leases/retries deterministic `review_preview` and `asset_preprocess` jobs, prepares 384-pixel review previews, creates sharded 192/384/768/1536 WebPs plus warranted 2560 detail files, persists complete extended metadata and transparent numeric quality primitives, uses accepted high-confidence non-RAW companions for RAW display where available, otherwise uses bounded RAW embedded previews, and persists video posters. Decoder byte/pixel/time limits are enforced with an isolated Pillow subprocess; ExifTool/FFmpeg calls have explicit timeouts. Derivatives are fsynced, atomically replaced only below the separate derivative root, reopened and SHA-256/size verified before ready state, and invalidated to `stale` by analyzer/input mismatch. `run_preprocessing_job` owns the existing writer lock. Review approval now requires every included media item to have a persisted ready or unavailable preview state. The legacy GET-only dashboard serves a certified ready derivative or pre-existing legacy cached JPEG; missing work returns `preparing`/`unavailable` without reading media, hashing, decoding, subprocesses, or database mutation, and the existing `ui` command accepts an optional `--derivative-root` for non-default prepared storage.
- Acceptance evidence: `test_asset_preprocessing_persists_oriented_derivatives_metadata_features_and_is_idempotent` proves orientation, all four base sizes, checksum/size/stat certification, sharded separation, indexed request lookup, complete GPS/exposure/equipment/software/raw metadata, bounded versioned quality values, deterministic rerun checksums, and unchanged canonical evidence. `test_numeric_feature_golden_data_and_warranted_detail_derivative` proves exact black-frame histogram/entropy/near-black/blankness values, comparative checkerboard sharpness/entropy, and the warranted 2560 output. Parameterized corrupt/pixel-limit tests plus the time-limit test persist honest derivative/feature error and corruption evidence. RAW companion/fallback and video-provider fixtures prove truthful `nonraw_companion`, `raw_embedded`, and `video_poster` states. Atomic-replace failure leaves the prior derivative intact; a post-output interruption requeues and adopts verified outputs; analyzer changes retain stale generations and create current replacements. The lock-owning entry point refuses a live writer. Review-preview preparation blocks approval until complete and preserves the synthetic inbox/canonical object. The HTTP test monkeypatches Pillow, subprocess creation, derivative hashing, and canonical `Path.open` to fail and still serves ready/legacy files while a queued derivative reports 425 without request-time work. The v5-copy migration test proves the three Stage 4 tables, required indexes, no Stage 5+ tables, and clean integrity/foreign keys; all prior migration/copy/discovery/dashboard/end-to-end tests remain green.
- Verification commands/results: pre-edit `.\.venv\Scripts\python.exe -m pytest -q` -> `39 passed, 1 warning in 49.28s`; pre-edit `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`; final `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py` -> `18 passed in 8.32s`; final `.\.venv\Scripts\python.exe -m pytest -q tests\test_preprocess.py` -> `10 passed, 1 warning in 8.69s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `51 passed, 1 warning in 62.23s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`; `.\.venv\Scripts\python.exe -m media_vault ui --help` -> exit 0 with the optional `--derivative-root` and all existing UI options. The Stage 4 audits reported `STAGE4_SCHEMA_SCOPE_AUDIT_OK version=6 required_tables=3 later_stage_tables=0 integrity=ok foreign_keys=0 indexes=4`, `STAGE4_HTTP_MEDIA_AUDIT_OK route=database_lookup+file_serving forbidden_media_calls=0 sqlite=mode_ro+query_only`, `STAGE4_FS_BOUNDARY_AUDIT_OK derivative_publication=fsync+atomic_replace isolated_decode_temp=true source_writes=0 canonical_writes=0 temp_unlinks_only=true`, `STAGE4_AUTHORIZATION_AUDIT_OK completed=4 next=5 dependencies=stage2+stage3 later_stage_markers=0 record_fields=11`, `TEST_CAPTURE_CONFIG_AUDIT_OK enabled_screenshots=0 enabled_videos=0 enabled_traces=0 config_files_checked=0`, and `BROWSER_ARTIFACT_AUDIT_OK screenshots=0 videos=0 traces=0 repository_files_checked=36`. The derivative request query-plan assertion uses `idx_derivatives_ready_subject` with no derivative-table scan. `git diff --check` exited 0 with only advisory LF-to-CRLF working-copy notices. TypeScript/Svelte, Vitest, production-build, Playwright, and accessibility commands remain not applicable because Stage 4 deliberately adds no SvelteKit project or browser automation. The unchanged warning is the approved pre-existing Starlette TestClient deprecation.
- Immutability audit: every Stage 4 media operation uses isolated temporary synthetic inbox, vault, canonical-object, and derivative roots. Tests snapshot source/canonical entry kind, size, mtime/ctime nanoseconds, Windows attributes, and SHA-256 content around review preview generation, approval, oriented/image/RAW/video preprocessing, numeric analysis, corrupt/resource-limit failure, rerun, atomic failure, interruption/restart, analyzer invalidation, active-writer refusal, and HTTP serving; all snapshots match. Only regenerable derivative-root files are created/replaced, and cleanup unlinks only Stage 4 temporary derivative files. No source or canonical object is modified, moved, renamed, overwritten, or deleted. No real inbox, immutable source, live manifest, or canonical vault object was enumerated, opened, decoded, hashed, copied, analysed, modified, moved, renamed, overwritten, or deleted, and no live-vault migration/backfill was attempted.
- No-screenshot audit: no screenshot, video capture, browser trace, browser automation, visual snapshot, or capture-producing command was used. Tests use numerical synthetic image/video/RAW payloads and DOM/HTTP assertions only; no generated media was visually inspected or compared. Configuration and repository artifact-name audits found no enabled screenshot/video/trace setting and no corresponding browser artifact.
- Decisions/limitations: Stage 4 remains headless. Public `preprocess`, `worker`, `inbox-scan`, and `review-ui` commands plus `/api/v1`, SvelteKit, preferences, saved views, and new feature screens remain assigned to Stage 5. The Stage 4 service exposes explicit batch/asset enqueue and lock-owning execution contracts; Stage 5 will provide user-facing orchestration. Unavailable ExifTool/FFmpeg/embedded RAW/video/decode capabilities produce persisted errors and never request-time fallback work. Existing Stage 3-only progress samples retain explicit unavailable metadata/thumbnail values rather than being rewritten. No live migration, legacy-vault derivative backfill, or canonical/source maintenance was performed. NumPy 2.5.1 is now a direct exact dependency because Stage 4 imports it for transparent numeric feature calculations; it was already present in the exact runtime lock.
- Stopping point respected: Yes — stopped with safe preprocessing, persisted browser-visible media/metadata/base-quality inputs, and the refactored legacy preview path; no separate Svelte application, `/api/v1` review route, public Stage 5 command, user preference state, Stack profile, junk logic, facet/map rollup, or later-stage interface was added.
- Next authorized stage: Stage 5 only — Separate SvelteKit application and API foundation.

### 2026-07-21 — Stage 5: Separate SvelteKit application and API foundation

- Status: Complete
- Objective result: Added the independent production-built Svelte 5/SvelteKit/TypeScript application shell and its localhost-only typed `/api/v1` FastAPI boundary, with durable SQLite preferences and saved views, common envelopes, security controls, background command entry points, and accessible application foundations while preserving the existing read-only dashboard and stopping before feature-screen implementation.
- Changed: `media_vault/db.py`, new `media_vault/review_api.py`, new `media_vault/review_runtime.py`, `media_vault/cli.py`, `pyproject.toml`, new locked `review_ui/` project and tests, generated production assets under `media_vault/review_ui_dist/`, `tests/test_migrations.py`, new `tests/test_review_api.py`, new `tests/test_review_runtime.py`, new synthetic-browser support server `tests/support_review_server.py`, `.gitignore`, `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 7 additively adds only `review_application_state`, `user_preferences`, `saved_views`, and `api_idempotency_records`, plus the saved-view and idempotency indexes; versions 2–6 remain readable without implicit migration and the review application explicitly requires v7. `review-ui` serves the self-contained static application on localhost port 8766 under `/_app` with API routes under `/api/v1`, independently of legacy `ui` port 8765 and its `/static`/`/api` namespaces. Public `worker`, `preprocess`, and `inbox-scan` commands delegate to the existing lock-owning reviewed-copy, preprocessing, and discovery services. The API provides system, preferences, saved-view CRUD/keyset pages, and job-state reads with common schema/generation/request/page/job/unavailable/error envelopes; mutations are same-origin, JSON-only, payload-bounded, durably idempotent, and protected by global generation plus row revision. CSP is self-only without `unsafe-inline`, localhost hosts are enforced, CORS is not enabled, and handlers query persisted data or update application metadata only. `/api/v1/imports` and `/api/v1/library` are explicit 501 future-stage placeholders.
- Acceptance evidence: `test_review_app_is_separate_local_and_security_bounded` proves the legacy dashboard remains distinct, static/API namespaces do not overlap, localhost and CSP/security headers are enforced, foreign origins are rejected, and mutation responses use the common envelope. API tests also prove JSON and payload bounds, validation errors, migration-required state, restart persistence, optimistic generation/revision conflicts, durable idempotency/replay conflict, saved-view CRUD and strict keyset cursors, indexed saved-view seeks without a table scan or temporary sort, and typed job error/unavailable evidence. Runtime tests prove all four public command contracts and an idle worker that leaves synthetic immutable data unchanged. Vitest/Testing Library cover API paging, polling, stale-state handling, scroll restoration, theme controls, and navigation. Production DOM-only Playwright against the actual FastAPI/static build proves independent routing, CSP execution without violations, keyboard navigation and focus restoration, theme persistence across reload, semantic status, and zero automatically detectable axe violations on `/`, `/imports/`, `/library/`, and `/settings/`.
- Verification commands/results: pre-edit `.\.venv\Scripts\python.exe -m pytest -q` -> `51 passed, 1 warning in 75.79s`; pre-edit `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`; focused `.\.venv\Scripts\python.exe -m pytest -q tests\test_review_api.py tests\test_review_runtime.py tests\test_migrations.py` -> `28 passed, 1 warning in 12.41s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `61 passed, 1 warning in 80.40s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. `npm ci` installed the exact lock (`152` packages; `153` audited); `npm run check` -> `svelte-check found 0 errors and 0 warnings`; `npm test -- --run` -> `4` files and `7` tests passed; `npm run build` -> success and `Wrote site to "../media_vault/review_ui_dist"`; `npm run test:e2e` -> `1 passed (4.4s)`; `npm run test:a11y` -> `1 passed (5.5s)`. `npm audit --omit=dev --audit-level=low` -> `found 0 vulnerabilities`; `npm audit --audit-level=moderate` exited 0 and reported three low-severity development-only transitive findings described below. All four new CLI `--help` invocations exited 0 (`STAGE5_CLI_AUDIT_OK`). The schema/scope audit reported `STAGE5_SCHEMA_SCOPE_AUDIT_OK version=7 required_tables=4 later_stage_tables=0 integrity=ok foreign_keys=0`; the saved-view query-plan assertion uses `idx_saved_views_active_page` without a table scan or temporary ordering B-tree. The boundary audits reported `STAGE5_HTTP_MEDIA_AUDIT_OK mutation_tables=api_idempotency_records,review_application_state,saved_views,user_preferences forbidden_media_modules=0 request_media_work=0`, `TEST_CAPTURE_CONFIG_AUDIT_OK screenshot=off video=off trace=off`, `BROWSER_ARTIFACT_AUDIT_OK forbidden_capture_files=0 repository_files_checked=122`, and `SELF_CONTAINED_ASSET_AUDIT_OK external_runtime_resources=0 node_versions_exact=15`. The final ledger cross-check reported `FRONTEND_PROGRESS_AUDIT_OK completed=5 next=6 record_fields=11 dependencies=stage2+stage3+stage4+stage5 safety=aligned`. `git diff --check` exited 0 with advisory LF-to-CRLF notices only. The one Python warning is the unchanged pre-existing Starlette TestClient deprecation.
- Immutability audit: every Stage 5 API, command, worker, migration, and browser-server test uses isolated temporary synthetic vault/inbox/source/canonical fixtures. API and idle-worker tests snapshot relative entries, kind, size, mtime/ctime nanoseconds, attributes, and SHA-256 before and after the operations; every source and pre-existing canonical-object snapshot matched. Application mutations write only the four Stage 5 SQLite metadata tables. The production browser server creates an empty temporary manifest and no media. No real inbox, immutable source, live manifest, derivative, or canonical object was enumerated, opened, hashed, copied, decoded, analysed, modified, moved, renamed, overwritten, or deleted; no live migration or backfill was attempted.
- No-screenshot audit: Playwright is explicitly configured with `screenshot: 'off'`, `video: 'off'`, and `trace: 'off'`; smoke and accessibility runs use DOM, response-header, console, focus, persistence, and axe assertions only. No screenshot, video, trace, visual snapshot, or screenshot comparison was taken, generated, saved, inspected, or compared. The final artifact-name audit found zero forbidden capture artifacts across 122 checked repository files.
- Decisions/limitations: Stage 5 intentionally provides functional placeholders rather than the Stage 6 import or Stage 7 library interfaces, and TanStack Svelte Virtual is pinned for the later bounded feature lists but is not used by a placeholder. The API exposes only Stage 5 foundation state plus persisted job status; it performs no request-time media work. The complete npm tree has three low-severity development-only findings in SvelteKit's transitive `cookie <0.7.0`; the production dependency audit is clean, the current exact SvelteKit 2.70.1 remains pinned, and npm's proposed forced remedy is an inappropriate breaking downgrade to SvelteKit 0.0.30. The pre-existing Starlette warning remains documented. Live schema migration, worker processing, inbox discovery, and derivative backfill remain deliberately unexecuted.
- Stopping point respected: Yes — stopped with the production-built shell, API client, durable preferences/saved views, orchestration entry points, and security/accessibility foundations. Import and library routes contain only explicit future-stage placeholders; no Stage 6 import interface, Stage 7 library screen, photo user state, facet/map rollup, Stack, junk, grouping, ranking, or other later-stage feature was implemented.
- Next authorized stage: Stage 6 only — Import progress, review, history, and manifest interface. Its Stage 2, Stage 3, Stage 4, and Stage 5 dependencies are complete.

### 2026-07-21 — Stage 6: Import progress, review, history, and manifest interface

- Status: Complete
- Objective result: Delivered the independently usable visual reviewed-import workflow on the separate Svelte application: inbox discovery; permanent history/comparison; phase, folder, sample, event, and error evidence; bounded virtualized manifest review; persisted previews; metadata-only include/exclude and undo; explicit background preflight, approval, and execute gates; verified-copy progress/control; and durable reload/reconnect reconstruction from SQLite.
- Changed: `media_vault/db.py`; new `media_vault/review_stage6.py`; `media_vault/review_api.py`, `media_vault/review_copy.py`, and `media_vault/review_runtime.py`; `review_ui/src/lib/api.ts`, `review_ui/src/app.css`, and `review_ui/src/test-setup.ts`; new `ManifestReview.svelte`, `ImportTelemetry.svelte`, `ImportApproval.svelte`, and `UnavailableMetrics.svelte` components and their DOM tests; the completed `review_ui/src/routes/imports/+page.svelte` and reconnect test; the regenerated self-contained `media_vault/review_ui_dist/`; `tests/test_migrations.py`, `tests/test_review_api.py`, and new `tests/test_review_import_api.py`; `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 8 additively adds only `import_manifest_views` and `import_manifest_view_items`, their lookup/item indexes, and keyset indexes for import history, events, and errors; versions 2–7 remain readable without implicit migration. `/api/v1/imports` now provides bounded list/detail, comparison, folders, persisted samples, filterable events/errors, common indexed manifest pages, background arbitrary-query materialization, certified ready-preview serving, optimistic bulk decisions, background approval capacity preflight, approval, background execute authorization, and reviewed-copy controls. Discovery, capacity measurement, arbitrary sorting/searching, execute authorization, copying, and media preparation remain durable worker jobs. Request handlers only read persisted evidence, update metadata, enqueue jobs, or serve an existing certified derivative. The import interface provides explicit incremental keyset paging for every long evidence surface and keeps Stage 7 `/api/v1/library` as a 501 placeholder.
- Acceptance evidence: the synthetic end-to-end API workflow discovers/enqueues without publication, reconciles batch discovered totals exactly with the root folder and manifest, serves only a persisted derivative, applies exclusion with optimistic revisions, materializes path search plus safe multi-sort in the worker, records exact approval counts/capacity, refuses request-time capacity measurement, requires separate approval and execute booleans, pauses/resumes the reviewed-copy job, completes verified copying, preserves the excluded source, and creates exactly one expected new canonical object while leaving the pre-existing canonical object unchanged. History keyset paging and comparison keep completed batches inspectable. Query-plan assertions use `idx_import_batches_history_page` and the manifest-view primary key without a temporary ordering B-tree. DOM tests cover pointer brush, keyboard ranges, selection totals, path/classification/outcome filters, safe secondary sort, metadata-only decision language, undo, explicit approval, stale-state handling, reconnect, unavailable legacy metrics, and keyset cursor forwarding. Persisted progress samples alone drive the throughput/discovery/error/queue graph.
- Verification commands/results: pre-edit `.\.venv\Scripts\python.exe -m pytest -q` -> `61 passed, 1 warning in 63.03s`; pre-edit `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`; focused Stage 2–6/backend compatibility run -> `58 passed, 1 warning in 37.83s`; final migration plus Stage 6 API run `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py tests\test_review_import_api.py` -> `23 passed, 1 warning in 7.28s`; hardened Stage 6 workflow run `.\.venv\Scripts\python.exe -m pytest -q tests\test_review_import_api.py` -> `3 passed, 1 warning in 1.74s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `65 passed, 1 warning in 53.89s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. `npm ci` installed the exact lock (`152` packages; `153` audited); final `npm run check` -> `0 errors and 0 warnings`; final `npm test -- --run` -> `8` files and `14` tests passed; final `npm run build` -> success and `Wrote site to "../media_vault/review_ui_dist"`; final `npm run test:e2e` -> `1 passed (3.4s)`; final `npm run test:a11y` -> `1 passed (4.9s)`; `npm audit --omit=dev --audit-level=low` -> `found 0 vulnerabilities`; `npm audit --audit-level=moderate` exited 0 with the three unchanged low-severity development-only transitive findings described below. The audits reported `STAGE6_SCHEMA_SCOPE_AUDIT_OK version=8 required_tables=2 required_indexes=5 later_stage_tables=0 integrity=ok foreign_keys=0`, `STAGE6_CLI_COMPATIBILITY_AUDIT_OK commands=6`, `TEST_CAPTURE_CONFIG_AUDIT_OK screenshot=off video=off trace=off`, `BROWSER_ARTIFACT_AUDIT_OK forbidden_capture_files=0 browser_output_files=1`, `SELF_CONTAINED_SOURCE_AUDIT_OK external_runtime_resources=0`, and `FRONTEND_PROGRESS_AUDIT_OK completed=6 next=7 record_fields=11 dependencies=stage2+stage3+stage4+stage5`. `git diff --check` exited 0 with advisory LF-to-CRLF notices only. Intermediate validation found and corrected two test-only issues (invalid synthetic folder columns and an untyped Vitest mock); one npm invocation from the repository root produced the expected missing-`package.json` error and was immediately rerun from `review_ui`. The sole Python warning remains the approved pre-existing Starlette TestClient deprecation.
- Immutability audit: all Stage 6 discovery, API, job, approval, control, copy, migration, and UI-server tests use isolated temporary synthetic inbox, vault, derivative, and canonical fixtures. Stage 6 tests snapshot source and pre-existing canonical kind, size, mtime/ctime nanoseconds, mode, and SHA-256 before and after discovery, decisions, materialization, preflight, approval, execute, control, and verified copy; every snapshot matched. Discovery creates no object; the reviewed-copy test creates only its one expected content-addressed object and leaves excluded media untouched. HTTP approval/execute are explicitly tested with disk access replaced by a failing sentinel and consume persisted evidence only. No real inbox, immutable source, live manifest, derivative, or canonical object was enumerated, opened, hashed, copied, decoded, analysed, modified, moved, renamed, overwritten, or deleted, and no live migration/backfill was attempted.
- No-screenshot audit: Playwright remains explicitly configured with `screenshot: 'off'`, `video: 'off'`, and `trace: 'off'`; browser checks use DOM, response, keyboard/focus, reconnect, and axe assertions only. No screenshot, video, trace, visual snapshot, or comparison was taken, generated, saved, inspected, or compared. The browser-output audit found only Playwright's non-capture `.last-run.json` state file and zero forbidden capture artifacts.
- Decisions/limitations: common path-order plus one classification/decision/entry-kind filter runs as an immediate bounded indexed query; path search, outcome filters, reverse order, and compound sorts are normalized, revision-bound, and materialized in a durable background view. Pages are loaded incrementally rather than accumulating an unbounded manifest/history/evidence result. Approval capacity is measured in a background preflight and rechecked by the background execute authorization; no request measures storage or opens media. The existing three low-severity development-only findings remain in SvelteKit's transitive `cookie <0.7.0`; the production audit is clean, and npm's forced remedy is still an inappropriate breaking downgrade to SvelteKit 0.0.30. No live migration, import, copy, or preprocessing job was run. General vault browsing and all photo entity/state, library action, inspector, facet, map, Stack, ranking, and junk work remain unstarted.
- Stopping point respected: Yes — stopped once reviewed inbox importing was independently usable and fully validated. The library UI/API remains the Stage 5 placeholder, and no Stage 7 schema, endpoint, component, action, or materialization was added.
- Next authorized stage: Stage 7 only — Virtualized library, contact sheet, actions, filtering, and inspector. Its Stage 3, Stage 4, and Stage 5 dependencies are complete.

### 2026-07-21 — Stage 7: Virtualized library, contact sheet, actions, filtering, and inspector

- Status: Complete
- Objective result: Delivered the daily-use logical-photo vault browser and persisted-evidence inspector in the separate review application, with bounded virtualized/keyset pages, density and contact-sheet presentation, display-only grayscale, saved views and facets, immediate indexed and honest background compound orders, default rejected hiding, selection/keyboard controls, certified prepared derivatives, and metadata-only favourite/reject/rating actions.
- Changed: `media_vault/db.py`; new `media_vault/review_library.py`; `media_vault/review_api.py` and `media_vault/review_runtime.py`; `review_ui/src/lib/api.ts`, `review_ui/src/app.css`, the library route and overview copy; new `LibraryGrid.svelte`, `LibraryFilters.svelte`, and `LibraryInspector.svelte` components plus their DOM tests; API client/route tests; regenerated self-contained assets under `media_vault/review_ui_dist/`; `tests/test_migrations.py`, `tests/test_review_api.py`, and new `tests/test_review_library_api.py`; `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 9 additively adds only `photo_entities`, `photo_entity_members`, `photo_user_state`, `photo_user_state_events`, `materialized_views`, `materialized_view_items`, and `facet_rollups` plus immediate-sort, state, member, materialization, and facet indexes; versions 2–8 remain readable without implicit migration. A background catalog creates stable RAW-anchored or standalone entities only from verified persisted assets and accepts only completed, high-confidence, non-ambiguous RAW/JPEG evidence; persisted Stage 4 metadata selects the display asset. Common capture/import/filename/favourite/rejected/rating/quality/width/height/size/camera/lens/exposure/similarity/default-random orders use indexed keyset seeks; search, compound sorts, multi-valued filter combinations, and custom seeded random orders use durable background views bound to catalog and application-state generations. `/api/v1/library` now provides bounded pages, catalog preparation, five persisted facets, full entity evidence, ready derivative serving for the selected display asset, optimistic/idempotent/audited entity state, and database-resolved open-in-folder with no client path input. HTTP routes query persisted data, update SQLite metadata, enqueue work, serve certified derivatives, or launch Explorer for the database-selected file only; catalog construction and arbitrary ordering remain background jobs.
- Acceptance evidence: `test_library_catalog_api_actions_inspector_and_immutability` proves first-use background catalog preparation, stable logical IDs across a forced refresh, lock-safe orphaned-job recovery, bounded retry after a synthetic worker failure, a two-member RAW entity whose prepared response bytes come from the selected JPEG companion, facet reconciliation, bounded inspector evidence, explicit Stack/junk unavailable fields, default rejected hiding, include-rejected state reads, optimistic stale-state refusal, exact idempotency replay, state-generation invalidation and rematerialization of an arbitrary view, database-selected open-in-folder, rejection of a client-supplied path, restart persistence, action audit, and unchanged synthetic source/canonical evidence. `test_library_common_query_plan_and_generated_scale` creates exactly 146,034 logical entities, checks every one of the 15 common sort fields in both directions without `USE TEMP B-TREE`, proves the expected capture index on initial and cursor seeks, loads only 120 items per page, and verifies disjoint keyset pages; the existing one-million-row source-manifest query-plan test remains green. Library component/route tests cover bounded virtualization, derivative `srcset`, density, contact sheet, display-only grayscale, distinct RAW/Stack indicators, selection, keyboard-visible favourite/reject/rating/inspect/folder actions, persisted facets, default rejected hiding, multi-sort/background query inputs, complete inspector evidence/placeholders, open-folder control, saved preference restoration, and the 120-item page contract. Browser accessibility checks keep `/library/` free of automatically detectable axe violations.
- Verification commands/results: final `.\.venv\Scripts\python.exe -m pytest -q` -> `68 passed, 1 warning in 94.17s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. Focused Stage 7 migration/API/query-plan runs also passed (`29 passed, 1 warning` before final tightening; final `tests\test_review_library_api.py` -> `2 passed, 1 warning`; final recovery/retry workflow -> `1 passed, 1 warning in 1.97s`). `npm ci` installed the exact lock (`152` packages; `153` audited); `npm run check` -> `svelte-check found 0 errors and 0 warnings`; `npm test -- --run` -> `12` files and `19` tests passed; `npm run build` -> success and `Wrote site to "../media_vault/review_ui_dist"`; `npm run test:e2e` -> `1 passed (5.8s)`; `npm run test:a11y` -> `1 passed (5.5s)`; `npm audit --omit=dev --audit-level=low` -> `found 0 vulnerabilities`. The full `npm audit --audit-level=low` exited 1 only for the same three pre-existing low-severity development-only transitive `cookie <0.7.0` findings; npm's proposed forced remedy remains the unrelated breaking SvelteKit 0.0.30 downgrade. The boundary/scope audit reported `STAGE7_AUDIT_OK capture_config=off capture_artifacts=0 frontend_media_primitives=0 request_media_primitives=0 later_stage_tables=0 approved_docs=3`; the application-source audit reported `SELF_CONTAINED_APPLICATION_SOURCE_AUDIT_OK external_urls=0`; the final ledger audit reported `FRONTEND_PROGRESS_AUDIT_OK completed=7 next=8 record_fields=11 dependencies=stage4+stage7 safety=aligned`. Migration tests verify SQLite integrity and foreign keys on an isolated v8 copy, required Stage 7 tables/indexes and `state_generation`, and absence of Stage 8–10 tables. `git diff --check` exited 0 with advisory LF-to-CRLF notices only. The sole Python warning is the unchanged approved Starlette TestClient deprecation.
- Immutability audit: all Stage 7 catalog, API, action, refresh, migration, scale, and browser-server tests use isolated temporary synthetic source, vault, derivative, and canonical fixtures. The action workflow snapshots each synthetic source and canonical-object relative file, size, mtime/ctime nanoseconds, mode, and SHA-256 before catalog construction and after favourite/reject/rating, view materialization, open-folder resolution, forced catalog refresh, restart, and audit reads; every value matched. The selected folder opener is an injected path collector in tests. Stage 7 writes only SQLite state/materialization metadata and generated static UI output; it neither creates nor changes canonical objects. No real inbox, immutable source, live manifest, derivative, or canonical object was enumerated, opened, hashed, copied, decoded, analysed, ranked, grouped, modified, moved, renamed, overwritten, or deleted, and no live migration, backfill, or catalog job was attempted.
- No-screenshot audit: Playwright remains explicitly configured with `screenshot: 'off'`, `video: 'off'`, and `trace: 'off'`; component, route, smoke, and accessibility checks use DOM, response-header, console, focus, state, and axe assertions only. No screenshot, video, trace, visual snapshot, or comparison was taken, generated, saved, inspected, or compared. The final browser-output artifact-name audit found zero `.png`, `.jpg`, `.jpeg`, `.webp`, `.mp4`, `.webm`, or `.zip` capture artifacts.
- Decisions/limitations: Entity-level state is preserved through catalog refresh by stable anchor-derived IDs. A materialized arbitrary view includes the current global application generation, conservatively invalidating it after any application mutation as well as photo-state changes. Similarity order uses only already-persisted Stage 4/legacy similarity evidence and performs no grouping or ranking. The Stage 7 card contract and DOM test distinguish RAW and Stack indicators, but API Stack membership remains honestly zero/unavailable until Stage 9 authorizes persisted Stack profiles; inspector junk evidence remains explicitly unavailable until Stage 10. Calendar, folder hierarchy, equipment views, offline map/clusters, adjustable Stacks, cover ranking/override, junk classification, feedback, bulk reject, and undo for these future operations were not implemented. The three unchanged low-severity development-only npm findings and the pre-existing Starlette warning remain documented. No live deployment or schema migration was run.
- Stopping point respected: Yes — stopped with the complete daily-use grid and inspector. No Stage 8 calendar/folder/equipment/map schema, API, materialization, basemap, or UI work; no Stage 9 adjustable Stack/ranking work; and no Stage 10 junk/bulk-reject work was started.
- Next authorized stage: Stage 8 only — Calendar, folder, equipment, and private map views. Its Stage 4 extended-metadata/GPS and Stage 7 logical-entity/materialized-view dependencies are complete.

### 2026-07-21 — Stage 8: Calendar, folder, equipment, and private map views

- Status: Complete
- Objective result: Added four alternate organization views driven entirely by persisted Stage 4 metadata and Stage 7 stable logical entities: exact/unknown/ambiguous calendar buckets, recursive source-root folder navigation with logical-photo and source-occurrence modes, normalized camera/lens drill-down with retained raw evidence, and a private offline map using bundled code-native land geometry plus persisted zoom/geohash clusters. Every date, folder, equipment, map-cluster, and unknown-location link resolves through persisted entity membership to a bounded background-materialized primary-library view with the same metadata-only user state.
- Changed: `media_vault/db.py`; new `media_vault/review_organization.py`; `media_vault/review_library.py`, `media_vault/review_api.py`, and `media_vault/review_runtime.py`; `review_ui/src/lib/api.ts`, `LibraryFilters.svelte`, `LibraryGrid.svelte`, their API/library tests, `ShellNav.svelte` and its test; new `review_ui/src/lib/data/offline-world-map.ts`, `OrganizationViews.svelte` and its DOM test, and the `/organize/` route and route test; the overview route, `app.css`, and accessibility route coverage; regenerated self-contained assets under `media_vault/review_ui_dist/`; `tests/test_migrations.py` and new `tests/test_review_organization_api.py`; `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 10 additively adds `calendar_buckets`, `calendar_bucket_items`, `folder_hierarchy_nodes`, `folder_hierarchy_items`, `equipment_rollups`, `equipment_rollup_items`, `map_entity_locations`, `map_unknown_location_items`, `map_clusters`, and `map_cluster_items`, with bounded calendar/month, hierarchy-child, equipment, geohash, viewport, and membership indexes; versions 2–9 remain readable without implicit migration. `organization_rollups_materialize` is a single-writer-guarded, recoverable background job bound to the ready library catalog generation. `/api/v1/organize` provides status/prepare, keyset calendar, folder, equipment, and bounded viewport-cluster reads; organization kind/key filters extend the existing background library-view contract. Requests only enqueue work or page persisted rollups/memberships. The bundled equirectangular basemap is application source, not derived media, and performs no external tile, coordinate, font, script, analytics, map, or CDN request.
- Acceptance evidence: the Stage 8 API workflow proves deterministic exact-date plus explicit unknown/ambiguous buckets; recursive source-root nodes whose logical counts do not multiply a second preserved occurrence while occurrence mode does; normalized camera/lens values with raw evidence and unknown buckets; zoom 0–18 deterministic geohashes, invalid-coordinate handling, two-sided antimeridian viewport results, persisted unknown-location membership, and stable cluster/entity links. Date and unknown-location links each enqueue a background library view and return exactly the expected stable entities; a favourite/rating mutation made through that view is immediately visible in the primary entity detail and changes SQLite metadata only. Forced rollup rebuilds retain deterministic bucket identities/counts, orphaned running work requeues safely, and SQLite integrity/foreign keys remain clean. Query-plan assertions use `idx_calendar_buckets_month`, `idx_folder_hierarchy_children`, `idx_equipment_rollups_page`, and `idx_map_clusters_viewport` without scanning `photo_entities` in request queries. DOM tests cover keyboard-focusable tabs, calendar navigation, unknown/ambiguous links, folder expansion and count modes, equipment drill-down, map zoom/pan, cluster/unknown links, restored viewport/mode, same-origin library route state, and the no-image offline basemap contract. DOM-only axe validation passes on `/organize/` and every existing route.
- Verification commands/results: dependency verification `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py tests\test_preprocess.py tests\test_review_library_api.py` -> `33 passed, 1 warning in 42.33s`; final focused `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py tests\test_review_organization_api.py` -> `24 passed, 1 warning in 8.43s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `71 passed, 1 warning in 80.59s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. `npm ci` installed the exact lock (`152` packages; `153` audited); final `npm run check` -> `0 errors and 0 warnings`; final `npm test -- --run` -> `14` files and `25` tests passed; final `npm run build` -> success and `Wrote site to "../media_vault/review_ui_dist"`; final `npm run test:e2e` -> `1 passed (3.3s)`; final `npm run test:a11y` -> `1 passed (5.7s)`; `npm audit --omit=dev --audit-level=low` -> `found 0 vulnerabilities`. Full `npm audit --audit-level=low` exited 1 only for the same three pre-existing low-severity development-only transitive `cookie <0.7.0` findings; npm's forced remedy remains the unrelated breaking SvelteKit 0.0.30 downgrade. Audits reported `STAGE8_SCHEMA_SCOPE_AUDIT_OK version=10 required_tables=10 required_indexes=6 later_stage_tables=0 integrity=ok foreign_keys=0`, capture configuration `screenshot=off video=off trace=off`, `FORBIDDEN CAPTURE ARTIFACT COUNT 0`, `EXTERNAL APPLICATION SOURCE URLS 0`, `STAGE 8 MEDIA-PRIMITIVE SOURCE MARKERS 0`, and a present production `organize/index.html`; `git diff --check` exited 0 with advisory LF-to-CRLF notices only. An intermediate accessibility run exposed an invalid pre-existing `aria-rowcount` on the empty Stage 7 grid; removing that unsupported attribute made the complete route audit pass without changing behavior. One intermediate npm test command was invoked from the repository root, produced the expected missing-`package.json` error, and was immediately rerun successfully from `review_ui`. The sole Python warning remains the approved pre-existing Starlette TestClient deprecation.
- Immutability audit: every Stage 8 migration, catalog, rollup, API, linked-view, shared-state, recovery, and rebuild test uses isolated temporary synthetic source roots, canonical objects, and SQLite/derivative state. The end-to-end Stage 8 workflow snapshots every synthetic source and canonical relative file, size, mtime/ctime nanoseconds, mode, and SHA-256 before organization enqueue/materialization and after all reads, linked-view jobs, favourite/rating state, forced rebuild, and integrity checks; every value matched. Stage 8 writes only SQLite rollup/materialization/preference/state records and generated static application output. No real source, live manifest, inbox, derivative, or canonical object was enumerated, opened, hashed, copied, decoded, analysed, modified, moved, renamed, overwritten, or deleted, and no live migration, backfill, or catalog job was attempted.
- No-screenshot audit: Playwright remains explicitly configured with `screenshot: 'off'`, `video: 'off'`, and `trace: 'off'`; component, route, smoke, and accessibility checks use DOM, response-header, console, focus, state, and axe assertions only. No screenshot, video, trace, visual snapshot, or comparison was taken, generated, saved, inspected, or compared. The final artifact audit found only Playwright's non-capture `.last-run.json` state and zero forbidden `.png`, `.jpg`, `.jpeg`, `.webp`, `.mp4`, `.webm`, `.zip`, screenshot, video, or trace artifacts.
- Decisions/limitations: the four views share one `/organize/` workspace and one catalog-generation-bound background rollup so their counts and links reconcile. Calendar navigation uses persisted ISO dates and never guesses unknown/ambiguous capture time. Folder roots remain separate, logical entities are the default, and occurrence mode explicitly audits preserved paths. Equipment normalization uses Unicode/whitespace/case normalization while retaining every raw make/model/lens value. The offline map intentionally uses a compact coarse world silhouette for orientation, stored geohash clusters for zoom 0–18, antimeridian-aware viewport queries, and a separate stored unknown-location membership; it provides no online tiles or geocoding. A forced Stage 8 refresh temporarily exposes its honest queued/building state until the same deterministic generation is ready. Adjustable Stack grouping/ranking/cover controls, junk classification/feedback, and bulk reject remain absent. The three development-only npm findings and pre-existing Starlette warning remain documented; no live deployment or schema migration was run.
- Stopping point respected: Yes — stopped when calendar, folder, equipment, and private offline map views were complete, used the same stable library entities/state, and linked back through persisted bounded views. No Stage 9 adjustable Stack profile, candidate-edge, grouping, ranking, cover override, schema/API/component, or Stage 10 junk/bulk-reject work was started.
- Next authorized stage: Stage 9 only — Similarity Stacks and best-frame ranking. Its Stage 4 feature/quality, Stage 5 background materialization, and Stage 7 logical-entity/browser dependencies are complete.

### 2026-07-21 — Stage 9: Similarity Stacks and best-frame ranking

- Status: Complete
- Objective result: Delivered adjustable persisted Stacks over stable logical-photo entities, with transparent perceptual/event evidence, bounded indexed candidate discovery, cached normalized profiles, deterministic background grouping, explainable best-frame ranking, and metadata-only cover overrides integrated into the primary library. The last ready profile remains usable while a new settings combination prepares, and ready, preparing, retry/error, and stale-profile states are explicit.
- Changed: `media_vault/config.py` and `media_vault/db.py`; new `media_vault/review_stacks.py`; `media_vault/review_runtime.py`, `media_vault/review_library.py`, and `media_vault/review_api.py`; `review_ui/src/lib/api.ts`, `review_ui/src/app.css`, the library route, `LibraryGrid.svelte`, and `LibraryInspector.svelte`; new `StackProfileControls.svelte` and its DOM test; updated grid, inspector, API-client, and library-route DOM tests; regenerated self-contained output under `media_vault/review_ui_dist/`; `tests/test_migrations.py`, `tests/test_review_library_api.py`, and new `tests/test_review_stacks_api.py`; `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 11 additively adds `stack_feature_inputs`, `stack_candidate_edges`, `stack_profiles`, `stacks`, `stack_members`, and `stack_cover_events` plus 13 bounded lookup/page/member indexes; versions 2–10 remain readable without implicit migration. `stack_profile_materialize` is a recoverable single-writer background job. It reads only certified existing derivatives plus persisted Stage 4/catalog evidence, persists versioned pHash, dHash, color distribution, aspect, capture time, equipment, normalized filename, RAW/JPEG confidence, relationship, quality, edit, motion, clipping, corruption, and resolution inputs, and limits locality candidates to 48 per entity instead of comparing all pairs. Normalized profiles cover similarity, time proximity, RAW/JPEG confidence, exposure, sharpness, motion, and display direction; profile/Stack IDs, memberships, order, ranking evidence, explanations, and analyzer/method versions are persisted. `/api/v1/stacks/profiles`, `/api/v1/stacks/status`, bounded Stack pages/details, and optimistic/idempotent cover updates expose only persisted results or enqueue work. Stack-aware library queries use the existing background materialized-view contract and persisted cover/ordinal rows. Cover overrides update SQLite metadata/audit events and application generation only.
- Acceptance evidence: identical normalized settings reuse the same profile and produce stable Stack IDs/order on repeated reads; loose defaults group the synthetic burst/composition while strict settings create more nearly-identical groups, and an unrelated time-adjacent frame remains separate. Direct persisted-evidence tests exercise time-proximity and RAW/JPEG-confidence boundaries, actual persisted pair-relationship confidence, darker/brighter exposure choices, freeze/intentional-blur motion choices, sharpness evidence, deterministic entity-ID ties, and the rule that an edited high-scoring frame cannot outrank an available unedited frame. Every cover carries a human-readable persisted explanation, `transparent-stack-cover-v1`, ranking inputs, and the original ranked explanation through override/reset. The end-to-end API test proves default and user profiles, previous-ready availability, background-only first preparation, exact idempotency replay, optimistic metadata-only override/reset, Stack-aware library materialization, member inspection/audit, restart recovery, analyzer-version invalidation, and unchanged synthetic media. A 2,000-entity scale test holds candidate edges below `48 * N` and far below all-pairs growth, while query plans use `idx_stack_inputs_phash_bucket` and `idx_stack_inputs_capture`. DOM tests cover all profile controls, ready/preparing/error states, RAW/Stack indicators, native keyboard-operable expansion, member strips, persisted explanations, and cover controls without client-side grouping or ranking.
- Verification commands/results: pre-edit dependency run `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py tests\test_preprocess.py tests\test_review_api.py tests\test_review_library_api.py` -> `40 passed, 1 warning in 44.25s`; pre-edit frontend `npm run check; npm test -- --run` -> `0 errors and 0 warnings`, `14` files and `25` tests passed. Focused `.\.venv\Scripts\python.exe -m pytest -q tests\test_review_stacks_api.py tests\test_review_library_api.py tests\test_migrations.py` -> `28 passed, 1 warning in 68.48s`; final relationship-evidence `.\.venv\Scripts\python.exe -m pytest -q tests\test_review_stacks_api.py` -> `3 passed, 1 warning in 9.66s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `75 passed, 1 warning in 97.62s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. `npm ci` installed the exact lock (`152` packages; `153` audited); final `npm run check` -> `0 errors and 0 warnings`; final `npm test -- --run` -> `15` files and `27` tests passed; final `npm run build` -> success and `Wrote site to "../media_vault/review_ui_dist"`; final `npm run test:e2e` -> `1 passed (6.8s)`; final `npm run test:a11y` -> `1 passed (8.7s)`; final `npm audit --omit=dev --audit-level=low` -> `found 0 vulnerabilities`. Full `npm audit --audit-level=low` exited 1 only for the same three pre-existing low-severity development-only transitive `cookie <0.7.0` findings; npm's forced remedy remains the unrelated breaking SvelteKit 0.0.30 downgrade. Audits reported `STAGE9_SCHEMA_SCOPE_AUDIT_OK version=11 required_tables=6 required_indexes=13 later_stage_tables=0 integrity=ok foreign_keys=0`, capture configuration `screenshot=off video=off trace=off`, `FORBIDDEN_CAPTURE_ARTIFACT_COUNT 0`, `EXTERNAL_APPLICATION_SOURCE_URLS 0`, `FRONTEND_MEDIA_PRIMITIVE_MARKERS 0`, `REQUEST_MEDIA_PRIMITIVE_MARKERS 0`, `FRONTEND_PROGRESS_AUDIT_OK completed=9 next=10 record_fields=11 dependencies=stage4+stage7+stage9 safety=aligned`, `BROWSER_OUTPUT_AUDIT_OK forbidden_capture_files=0 output_files=1`, and `STAGE9_BOUNDARY_AUDIT_OK approved_docs=3 external_urls=0 frontend_media_primitives=0 request_media_primitives=0 library_dist=True`; `git diff --check` exited 0 with advisory LF-to-CRLF notices only. The first full Python run exposed one relevant stale Stage 7 assertion that still expected Stack membership to be unavailable; the assertion was updated to require an available empty persisted collection and the complete suite then passed. One coarse frontend primitive audit matched only the word `canvas` in the approved Stage 8 offline-map comment; the refined executable-primitive audit returned zero. The sole Python warning is the unchanged approved Starlette TestClient deprecation.
- Immutability audit: every Stage 9 migration, profile, worker, grouping, ranking, API, materialized-view, recovery, invalidation, override, scale, and browser-server test uses isolated temporary synthetic source, derivative, vault, and canonical fixtures. The full Stack workflow snapshots every synthetic source and canonical relative file, size, mtime/ctime nanoseconds, mode, and SHA-256 before profile enqueue/materialization and after all reads, profiles, views, overrides, resets, recovery, and validation; every value matched. Perceptual inputs are generated only by the background worker from certified derivatives beneath the synthetic derivative root. Stage 9 writes SQLite features, edges, profiles, memberships, explanations, events, materialized views, and generated static application output only. No real source, live manifest, inbox, derivative, or canonical object was enumerated, opened, hashed, copied, decoded, analysed, ranked, grouped, modified, moved, renamed, overwritten, or deleted, and no live migration, backfill, catalog, or Stack job was attempted.
- No-screenshot audit: Playwright remains explicitly configured with `screenshot: 'off'`, `video: 'off'`, and `trace: 'off'`; component, route, smoke, and accessibility checks use DOM, response-header, console, focus, state, and axe assertions only. No screenshot, video, trace, visual snapshot, or comparison was taken, generated, saved, inspected, or compared. The final browser-output audit found zero `.png`, `.jpg`, `.jpeg`, `.webp`, `.mp4`, `.webm`, `.zip`, screenshot, or trace capture artifacts.
- Decisions/limitations: Stage 9 deliberately uses explainable pHash/dHash/color/aspect plus time/equipment/filename/relationship evidence and no opaque embedding model. Standalone entities do not receive a false RAW/JPEG-confidence boost; that input is retained only for accepted logical RAW/JPEG entities. Missing or changed derivatives persist an honest partial input/error and remain deterministic singleton candidates rather than causing request-time work. Candidate discovery is intentionally capped at 48 indexed/local candidates per entity, Stack member detail is bounded to 500 members, and cover-event detail is bounded to 100 events. Profiles are bound to catalog and analyzer versions; stale profile IDs are refused after catalog changes, while a same-catalog previous ready profile stays available until replacement completes. Cover overrides preserve the ranked cover/evidence and can reset to its original explanation. The three unchanged development-only npm findings and pre-existing Starlette warning remain documented. No live deployment or schema migration was run.
- Stopping point respected: Yes — stopped with default and user-created Stack profiles ready, explainable, recoverable, and integrated into the library. No junk signal/profile/effective-result/feedback table, classifier, automatic action, bulk-reject API, bulk-reject interface, or Stage 11 backfill/release work was implemented.
- Next authorized stage: Stage 10 only — Explainable junk filtering, feedback, and bulk reject. Its Stage 4 persisted quality, Stage 7 logical-entity/browser and metadata-only state, and Stage 9 persisted Stack dependencies are complete.

### 2026-07-21 — Stage 10: Explainable junk filtering, feedback, and bulk reject

- Status: Complete
- Objective result: Delivered a safe, explainable, persisted junk-review workflow over stable logical-photo entities. Default and user-created profiles materialize all approved independent signals in background, apply configurable confidence/reason/agreement/favourite safeguards, retain human explanations and better alternatives, accept audited false-positive/false-negative feedback, create separately versioned calibration successors only after sufficient feedback, and expose a separate virtualized metadata-only bulk-reject workflow with conflict detection and undo. Nothing is rejected or otherwise changed automatically.
- Changed: `media_vault/config.py`, `media_vault/db.py`, new `media_vault/review_junk.py`, `media_vault/review_runtime.py`, and `media_vault/review_api.py`; `review_ui/src/lib/api.ts`, `review_ui/src/app.css`, navigation/overview/library inspector and route integration, new `JunkProfileControls.svelte`, `JunkPreview.svelte`, `BulkRejectView.svelte`, `/junk/`, and `/bulk-reject/`, plus their API/component/route/accessibility tests; regenerated self-contained output under `media_vault/review_ui_dist/`; `tests/test_migrations.py`, `tests/test_review_stacks_api.py`, and new `tests/test_review_junk_api.py`; `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: schema version 12 additively adds only `junk_signals`, `junk_profiles`, `junk_effective_results`, and `junk_feedback`, with eight bounded entity/reason/profile/result/feedback indexes; versions 2–11 remain readable without implicit migration. `junk_profile_materialize` and `junk_profile_calibrate` are recoverable single-writer background jobs. The 17 approved signal reasons retain confidence, threshold, method/analyzer version, exact input identity, ready/partial state, structured evidence, and applicable better-alternative entity. Normalized profiles persist confidence threshold, enabled reasons, minimum agreement, favourite protection, analyzer/calibration lineage, counts, status, and errors; effective results persist stable order, exact qualified reasons, explanation, agreement, protection, and better alternative. `/api/v1/junk/profiles`, `/api/v1/junk/status`, bounded result/detail reads, and feedback mutations expose only persisted evidence or enqueue durable jobs. `/api/v1/library/bulk-reject`, `/api/v1/library/bulk-reject/undo`, and `/api/v1/stacks/{profile_id}/{stack_id}/reject-rest` use request idempotency keys, separate scoped stable action identities, optimistic global/row/Stack revisions, favourite and large-selection confirmations, event/undo audit, and SQLite metadata only. The exact affected Stack-rest membership must be loaded before its action enables. Current favourite changes update the affected persisted ready-profile result/count deltas without media work. Library detail now includes the ready persisted junk result and analyzer-matched signal set; the selected junk profile and review mode use the existing revision-safe `junk.review` SQLite preference contract.
- Acceptance evidence: numeric synthetic fixtures produce positive persisted evidence for every approved reason and prove that every effectively hidden entity has at least the configured number of stored qualified reasons. The default requires two agreeing signals, so an artistic dark fixture, screenshot/download evidence, and test-chart filename/path evidence cannot hide an item alone. Focus deficit and its related extreme-blur reason remain below the default automatic threshold without a sharper persisted Stack alternative; better-neighbour links and explanations are retained when one exists. Favourites remain visible while their underlying signals stay available, including when favourite state changes after materialization. The minimum first 19 feedback entries enqueue no calibration; entry 20 queues background calibration, whose separately versioned successor adjusts only the future threshold and preserves the ready parent for selection/rollback. Feedback stores the full versioned signal/input/evidence snapshot. Bulk tests prove exact idempotency replay, stale-revision refusal, favourite preservation/confirmation, more-than-100 confirmation, scoped action identity, metadata-only reject, exact undo links, restart persistence, and Stack-rest rejection that lists the exact affected membership while excluding and preserving the cover. Analyzer versions retain distinct current signal sets; same-analyzer input changes invalidate only prior current inputs. DOM tests cover preview-before-action, reasons/explanations/better alternatives, both feedback directions, threshold/reason/agreement/favourite controls and persisted profile selection, bounded filters, persisted Stack expansion, pointer brush, keyboard range, favourite/large confirmations, explicit apply, exact Stack-rest membership, and undo.
- Verification commands/results: pre-edit dependency verification `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py tests\test_preprocess.py tests\test_review_api.py tests\test_review_library_api.py tests\test_review_stacks_api.py` -> `44 passed, 1 warning in 77.68s`; pre-edit `npm run check; npm test -- --run` -> `0 errors and 0 warnings`, `15` files and `27` tests passed. Focused migration `.\.venv\Scripts\python.exe -m pytest tests\test_migrations.py -q` -> `24 passed`; focused Stage 10 `.\.venv\Scripts\python.exe -m pytest tests\test_review_junk_api.py -q` -> `2 passed, 1 warning in 4.10s`; compatibility `.\.venv\Scripts\python.exe -m pytest -q tests\test_migrations.py tests\test_preprocess.py tests\test_review_library_api.py tests\test_review_stacks_api.py tests\test_review_junk_api.py` -> `41 passed, 1 warning in 66.79s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `78 passed, 1 warning in 123.89s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`. `npm ci` added the exact locked `152` packages and audited `153`; final `npm run check` -> `0 errors and 0 warnings`; final `npm test` -> `19` files and `37` tests passed; final `npm run build` -> success and `Wrote site to "../media_vault/review_ui_dist"`; final `npm run test:e2e` -> `1 passed (3.2s)`; final `npm run test:a11y` -> `1 passed (7.9s)`; `npm audit --omit=dev --audit-level=low` -> `found 0 vulnerabilities`. Full `npm audit --audit-level=low` exited 1 only for the same three pre-existing low-severity development-only transitive `cookie <0.7.0` findings; npm's proposed forced remedy is the unrelated breaking SvelteKit 0.0.30 downgrade. Audits reported `STAGE10_SCHEMA_SCOPE_AUDIT_OK version=12 required_tables=4 required_indexes=8 integrity=ok foreign_keys=0`, `APPROVED_FRONTEND_DOC_COUNT 3`, capture configuration `screenshot=off video=off trace=off`, `FORBIDDEN_CAPTURE_ARTIFACT_COUNT 0`, `STAGE10_MEDIA_PRIMITIVE_SOURCE_MARKERS 0`, `STAGE10_REQUEST_MEDIA_PRIMITIVE_MARKERS 0`, `STAGE10_EXTERNAL_APPLICATION_URLS 0`, and present production `junk/index.html` and `bulk-reject/index.html`. `git diff --check` exited 0 with advisory LF-to-CRLF notices only. An intermediate pair of npm commands was invoked from the repository root, produced only the expected missing-`package.json` errors, and was immediately rerun successfully from `review_ui`. Focused review found and fixed stale same-version signal current flags, analyzer-mismatched detail evidence, shallow-focus leakage through the extreme-blur reason, post-materialization favourite staleness, incomplete false-negative feedback snapshots, scoped bulk-action identity, exact Stack-rest membership gating, and implicit large-Stack confirmation before the final suites. The sole Python warning remains the approved pre-existing Starlette TestClient deprecation.
- Immutability audit: every Stage 10 migration, profile, signal, effective-result, feedback/calibration, bulk action, Stack-rest action, recovery, invalidation, restart, and API test uses isolated temporary synthetic source, derivative, vault, and canonical fixtures. The end-to-end workflow snapshots every synthetic source and canonical relative file, size, mtime/ctime nanoseconds, mode, and SHA-256 before profile preparation and after profiles, feedback, calibrations, rejects, conflicts, undo, Stack-rest, favourite changes, restart, and validation; every value matched. The Stage 10 worker reads only persisted entity, Stage 4 feature, state, relationship, and ready Stack rows and writes only SQLite signal/profile/result/feedback/job metadata. HTTP and frontend code only page persisted evidence, update bounded metadata, or enqueue jobs. No real source, live manifest, inbox, derivative, or canonical object was enumerated, opened, decoded, hashed, copied, analysed, ranked, grouped, modified, moved, renamed, overwritten, or deleted, and no live migration, backfill, junk job, or deployment was attempted.
- No-screenshot audit: Playwright remains explicitly configured with `screenshot: 'off'`, `video: 'off'`, and `trace: 'off'`; component, route, smoke, and accessibility checks use DOM, request, focus, state, and axe assertions only. No screenshot, video, trace, visual snapshot, or comparison was taken, generated, saved, inspected, or compared. The final browser-output audit found zero forbidden capture artifacts.
- Decisions/limitations: Stage 10 intentionally uses transparent persisted numeric quality/relationship/Stack evidence and conservative filename/path markers rather than an opaque classifier. The default 0.72 profile requires two reasons, protects favourites, and does not let focus-derived signals cross the threshold without a sharper related frame. Calibration starts only after 20 feedback entries and adjusts a successor profile's confidence threshold by a bounded 0.05 step; it does not rewrite the parent or train an opaque model. The Junk and Bulk reject routes hold at most 120 results per page; filters apply to that bounded loaded page, Stack expansion reads at most 500 persisted members, and Stack-rest refuses more than 499 affected members so its complete cover-plus-membership remains within that bound. A selection above 100 and any favourite require separate explicit confirmations. Prior profiles and analyzer-version evidence remain queryable for rollback/audit. The three development-only npm findings and pre-existing Starlette warning remain documented for Stage 11; no forced dependency downgrade was made. No live deployment, schema migration, backfill, or release work was run.
- Stopping point respected: Yes — stopped when junk review, feedback/calibration, bulk rejection, Stack-rest rejection, and undo were explainable, reversible, persisted, and fully verified. No Stage 11 live-vault migration, copied-database/full backfill, compatibility release work, performance/backfill audit, or deployment was begun.
- Next authorized stage: Stage 11 only — Backfill, compatibility, performance, accessibility, and release verification. Its Stage 1–10 dependencies are complete; its live-writer, copied-database, backup/free-space/integrity, and immutability gates must be re-verified before any maintenance.

> [!WARNING]
> The Stage 11/maintenance conclusions below are historical evidence and were directly superseded by the later review. In particular, “safe,” “resumable,” “release verified,” and “authorized maintenance” claims are invalid for live operation because job recovery, backfill scope/inventory/retries, writer exclusion, publication, workflow invalidation, and restore proof are incomplete.

### 2026-07-22 — Stage 11: Backfill, compatibility, performance, accessibility, and release verification

- Status: Complete
- Historical objective result: Added the separate review application and a schema-neutral `vault_backfill` coordinator intended to prepare verified assets in bounded low-priority batches and materialize catalog/organization/Stack/junk/history state. The stage recorded progress/pause/resume behaviour and isolated tests, followed by a live schema migration and a paused inventory. **Superseding review:** the coordinator does not safely recover every job, isolate its job kinds, freeze a complete inventory, count every retry, or connect later imports to current projections; neither the live pause nor the migration proves a safe resumable release state.
- Changed: new `media_vault/review_backfill.py`; `media_vault/config.py`, `media_vault/preprocess.py`, `media_vault/review_runtime.py`, and `media_vault/review_api.py`; `review_ui/src/lib/api.ts`, `review_ui/src/routes/settings/+page.svelte`, `review_ui/src/routes/+page.svelte`, and `review_ui/src/app.css`; new `review_ui/src/routes/settings/page.test.ts` plus API-client coverage; regenerated self-contained production output under `media_vault/review_ui_dist/`; new `tests/test_review_backfill.py`; `README.md`, `SCHEMA.md`, and this ledger. No schema migration, legacy dashboard, media identity, copy/publication, recovery, sidecar, export, or existing CLI contract was removed or redirected.
- Contracts: Stage 11 adds no schema version or table. `vault_backfill` reuses version-12 `background_jobs` at coordinator priority 1; each current asset batch is limited by `WorkerLimits.backfill_batch_size` (default 32, validated 1–500) and uses priority 5 while reviewed-copy work retains priority 100. Persisted progress includes coordinator version/phase/message, verified-asset total/cursor/enqueued/completed/failed/unavailable counts, active child IDs, completed phases, legacy-history count, and throttle evidence. `preprocess --backfill` creates/resumes and drains the complete supported release chain; the ordinary `preprocess` contract remains preprocessing-only. `GET /api/v1/backfill` reads persisted SQLite state, and idempotent optimistic `POST /api/v1/backfill/control` performs only start/pause/resume metadata updates or a small coordinator enqueue. The worker alone inventories assets, enqueues preprocessing, or materializes results. Pause wins over a concurrent coordinator-slice completion and resume repairs any queued/pause state. `audit_release_state` validates schema/integrity/foreign keys, certified ready-derivative size/SHA-256, catalog/materialization generation lineage, and persisted backfill state while opening only SQLite and existing regenerable derivatives, never source or canonical media.
- Acceptance evidence: the copied-vault end-to-end test migrates schema 2→12 with a verified backup, completes every backfill phase, interrupts/recovers the coordinator, proves pause/resume and reviewed-import priority, creates ready derivatives/features/catalog/facets/common views/organization/default Stack/default junk/legacy history, verifies derivative checksums and materialized generations, syncs sidecars, exports JSONL, rebuilds a recovery index, and retains identical source plus original/copied canonical hash/stat snapshots. API tests monkeypatch `Path.open` after app construction and prove start/idempotent replay/pause/resume/job reads perform no media open. The pause-race regression proves a concurrent control cannot be overwritten by a worker slice. Existing migration, fault-injection, API contract, query-plan, million-row/six-figure scale, recovery, sidecar/export, import, library, organization, Stack, junk, dashboard, and CLI suites all remain green. Settings DOM tests cover persisted counts/progressbar, native keyboard focus/action, status messages, offline-safe refresh, pause/resume, and honest unavailable states; full route accessibility and keyboard smoke checks pass with reduced-motion/responsive/contrast/long-path/Unicode behavior covered by application CSS and synthetic fixtures.
- Verification commands/results: pre-edit `.\.venv\Scripts\python.exe -m pytest -q` -> `78 passed, 1 warning in 114.39s`; pre-edit Ruff -> `All checks passed!`; pre-edit `npm run check` -> `0 errors and 0 warnings`; pre-edit `npm test` -> `19` files and `37` tests passed. Focused compatibility `.\.venv\Scripts\python.exe -m pytest -q tests\test_review_backfill.py tests\test_migrations.py tests\test_preprocess.py tests\test_review_runtime.py tests\test_review_api.py tests\test_review_library_api.py tests\test_review_organization_api.py tests\test_review_stacks_api.py tests\test_review_junk_api.py` -> `54 passed, 1 warning in 61.65s`; focused final backfill/race suite -> `3 passed, 1 warning in 3.67s`; final `.\.venv\Scripts\python.exe -m pytest -q` -> `81 passed, 1 warning in 119.54s`; final Ruff -> `All checks passed!`. `npm ci` installed the exact lock (`152` packages; `153` audited); final `npm run check` -> `0 errors and 0 warnings`; final `npm test` -> `20` files and `39` tests passed; final `npm run build` -> success and `Wrote site to "../media_vault/review_ui_dist"`; final DOM-only `npm run test:e2e` -> `1 passed (4.8s)`; final `npm run test:a11y` -> `1 passed (8.3s)`; production dependency audit -> `found 0 vulnerabilities`. Full npm audit still exits 1 only for the same three pre-existing low-severity development-only transitive `cookie <0.7.0` findings whose forced remedy would install the unrelated breaking SvelteKit 0.0.30. The unchanged Starlette TestClient warning still recommends `httpx2`; no dependency churn was introduced. `git diff --check` exited 0 with advisory LF-to-CRLF notices only.
- Live migration/backfill verification: the consistent isolated live-manifest copy contained 146,034 assets and exactly 3,615,932,416 bytes; SQLite backup completed in 333.24 seconds. Explicit copied-vault migration applied versions 3–12 in 367.4 seconds, retained a verified schema-v2 backup, and returned integrity `ok`, zero foreign-key issues, and schema 12. Immediately before live migration the writer lock was absent and G: had 1,954,203,643,904 free bytes. Explicit live migration applied versions 3–12 in 557.5 seconds and retained `G:\MediaVault\state\backups\manifest-v2-20260722T010355Z-3d51a7116cd2.sqlite3`, size 3,615,932,416, SHA-256 `b4db14244726bdae77360266d4136196b5b30384559c8fee8b91ce979e339981`, schema 2, integrity `ok`, and zero foreign-key issues; the resulting live schema-12 database also returned integrity `ok` and zero foreign-key issues. Live release audit again returned integrity `ok`, zero foreign-key issues, zero derivative checksum errors, and honest absent catalog/materializations. Live backfill job `job1_1edcbc628d82eb5693a3902ae5763098b8da30e67fc5f82c5ee5e03252812ca8` is paused at inventory with 32/146,034 asset jobs enqueued, 0 completed, 0 failed, 0 unavailable; all 32 children are queued/control-paused at priority 5 and no bulk media job was run.
- Immutability audit: every Stage 11 automated workflow uses isolated synthetic source/canonical/derivative roots and compares path, size, mtime/ctime nanoseconds, mode, and SHA-256 before and after migration, complete backfill, sidecar/export, and recovery; all snapshots matched. Before the live operation, a complete manifest identity digest covered 146,034 asset/hash/object-path rows (`787dfdee260c35d695d3a13541c868eee03a8e5374481d1c20c6f09608beed1e`) and 1,374,328 source identity/stat rows (`d70fb7dfba153f8f47fbcc3287fad9fb201ba304e8f76945346f3073f65968d2`). Every one of 146,034 canonical paths was present and beneath `objects`; their complete size/mtime/ctime/mode digest was `8c02bde9964f72e65bf168ddea0bb058c5d42fc5a1717fc6bf6310bba367042f`, with zero missing/outside paths. A deterministic 64-object bounded sample had zero manifest SHA-256 mismatches. After live migration plus bounded coordinator enqueue/pause, the asset, source-record, object-count, complete canonical stat, and all 64 sampled content/stat values matched exactly; no writer lock remained. Migration opened only SQLite state/backup files; the coordinator slice performed persisted inventory and canonical path/stat validation only. No source or canonical object was modified, moved, renamed, overwritten, or deleted.
- No-screenshot audit: Playwright remained explicitly configured with `screenshot: 'off'`, `video: 'off'`, and `trace: 'off'`. Component, route, smoke, accessibility, and manual review used DOM/source/focus/response/state/axe evidence only. No screenshot, video, browser trace, visual snapshot, or comparison was taken, generated, saved, inspected, or compared. The final browser-output audit found only `.playwright-output/.last-run.json` and `FORBIDDEN_CAPTURE_ARTIFACT_COUNT=0`; frontend external-URL and executable media-primitive audits both returned zero, as did the refined Stage 11 request-path media-primitive audit.
- Historical decisions/limitations: Stage 11 was schema-neutral and the job was paused after one bounded enqueue slice with zero processed assets and no catalog/materializations. The ledger originally said it could be resumed from Settings/worker/preprocess and that no unsafe behaviour remained. **Superseding review:** do not resume it. The generic worker can claim unrelated copy work, running jobs can be stranded, inventory/retries/generation invalidation are unsafe, and the same-disk retained migration backup is not complete disaster recovery. The recorded dependency warnings and test evidence remain factual point-in-time evidence only.
- Historical stopping-point claim: the stage stopped after building the application, recording its checks/migrations, and pausing the live backfill. The later review invalidated “fully verified” and “resumable” as release conclusions.
- Current next authorized work: documentation/rework on isolated synthetic or proven path-isolated copied state only. Resuming the persisted Stage 11 backfill is **not authorized**.

### 2026-07-22 — Stage 11 maintenance: one-click live resume and persisted ETA

- Status: Complete
- Historical objective result: Added `Resume Live Vault Backfill.cmd` to open the separate review interface and run `preprocess --backfill`, plus persisted ETA display. **Do not use the launcher:** the later review found that this generic worker can claim unrelated job kinds and that interruption/recovery is not safe across the full workflow.
- Changed: new `Resume Live Vault Backfill.cmd`; `media_vault/review_backfill.py`, `media_vault/review_runtime.py`, `review_ui/src/lib/api.ts`, `review_ui/src/routes/settings/+page.svelte`, `review_ui/src/routes/settings/page.test.ts`, `review_ui/src/app.css`, regenerated `media_vault/review_ui_dist/`, `tests/test_review_backfill.py`, `tests/test_review_runtime.py`, `README.md`, `SCHEMA.md`, and this ledger.
- Contracts: no schema, migration, job kind, API route, priority, media identity, or publication contract changed. Coordinator progress JSON now retains at most 128 positive completed-attempt durations plus cumulative timing-sample count, current/EWMA asset rate, ETA seconds, confidence, and evidence-basis text. Remaining work counts both successful and terminal failed asset jobs as processed. ETA covers active asset preprocessing only and explicitly excludes pause time, higher-priority queue starvation, and final catalog/organization/Stack/junk phases. `preprocess --backfill` still creates or drains the existing release chain but now repairs a persisted paused state through the established metadata-only resume control before starting the worker.
- Acceptance evidence: deterministic ETA tests prove `learning` with fewer than three timing samples and `high` confidence for ten stable samples; the copied synthetic backfill proves timing evidence is durably persisted and reaches zero remaining asset time; the runtime test proves `preprocess --backfill` resumes the same persisted job ID without media work; the launcher audit proves it uses `review-ui --no-worker` plus the existing backfill command and contains no migration action. Settings DOM coverage proves the progress bar uses processed assets and displays the formatted active-time estimate and confidence. Generated production output contains the shipped ETA interface.
- Verification commands/results: focused backend `.\.venv\Scripts\python.exe -m pytest -q tests\test_review_backfill.py tests\test_review_runtime.py` -> `8 passed, 1 warning in 3.89s`; focused launcher runtime -> `5 passed in 2.18s`; focused Ruff -> `All checks passed!`; focused `npm run check` -> `0 errors and 0 warnings`; focused Vitest -> `2` files and `11` tests passed. Final `.\.venv\Scripts\python.exe -m pytest -q` -> `84 passed, 1 warning in 111.48s`; final `.\.venv\Scripts\python.exe -m ruff check .` -> `All checks passed!`; final `npm run check` -> `0 errors and 0 warnings`; final `npm test` -> `20` files and `39` tests passed; final `npm run build` -> success and wrote `media_vault/review_ui_dist`; capture settings were verified off before final DOM-only `npm run test:e2e` -> `1 passed (3.4s)` and `npm run test:a11y` -> `1 passed (8.2s)`. The sole Python warning remains the documented pre-existing Starlette TestClient/httpx warning.
- Immutability audit: all new worker/ETA tests use empty or isolated synthetic vault state; the copied-backfill test retains its exact source and original/copied canonical hash/stat comparisons. ETA calculation consumes only persisted SQLite attempt timestamps. The launcher was inspected and tested but deliberately not executed. A final read-only query of the live manifest confirmed job `job1_1edcbc628d82eb5693a3902ae5763098b8da30e67fc5f82c5ee5e03252812ca8` remains paused at inventory with 32/146,034 enqueued, zero completed/failed, and all 32 child jobs queued/control-paused. No live media job ran and no live source or canonical object was opened, decoded, hashed, copied, analysed, ranked, grouped, modified, moved, renamed, overwritten, or deleted.
- No-screenshot audit: Playwright capture settings remained `screenshot: 'off'`, `video: 'off'`, and `trace: 'off'`; tests used DOM, keyboard, response, persisted-state, and accessibility evidence only. No screenshot, video, browser trace, visual snapshot, or comparison was taken, generated, saved, inspected, or compared.
- Historical decisions/limitations: the launcher is workstation-specific and starts one worker plus a no-worker UI. The ledger originally called closing the worker a safe interruption and rerunning it a safe resume. **Superseding review:** that claim is false for the current job system; closing the worker can strand running work, and the launcher can process unrelated queued jobs. ETA limitations remain point-in-time implementation evidence, not authorization.
- Stopping point respected: Yes — stopped after producing and verifying the requested resume launcher and persisted ETA. The live job was not resumed, no later feature was begun, and the existing Stage 11 resumable stopping point remains intact.
- Current next authorized work: none on live data. Running the launcher is explicitly prohibited by [SAFETY_HOLD.md](SAFETY_HOLD.md).
